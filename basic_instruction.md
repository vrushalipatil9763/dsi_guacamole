This project is just for the introduction of git commands.
How do you execute the commands, possibly you can do automation using shell scripting.
Here we will learn command like git status, git add, git commit, git push etc.
There are few more advanced commands where you can use git reset, rollback, version change etc.